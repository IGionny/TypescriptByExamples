//THIS is NOT a module function but only a function inside a file that is part of a project
//It's a Developer responsability to include/load it at runtime (in browser or nodejs app)
function IsEmpty(value: string) : boolean{
    return (value === "");
}